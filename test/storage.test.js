describe("Compute Services", () => {
  test("Create storage", () => {
    expect({ one: 1, two: 2 }).toEqual({ one: 1, two: 2 });
  });

  test("Stop storage", () => {
    expect({ one: 1, two: 2 }).toEqual({ one: 1, two: 2 });
  });

  test("Start storage", () => {
    expect({ one: 1, two: 2 }).toEqual({ one: 1, two: 2 });
  });

  test("Reboot storage", () => {
    expect({ one: 1, two: 2 }).toEqual({ one: 1, two: 2 });
  });

  test("Destroy storage", () => {
    expect({ one: 1, two: 2 }).toEqual({ one: 1, two: 2 });
  });
});
